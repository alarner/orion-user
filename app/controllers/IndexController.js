module.exports = {
	login: function(req, res, models, config) {
		res.view();
	},

	logout: function(req, res, models, config) {
		res.view();
	},

	register: function(req, res, models, config) {
		res.view();
	}
};